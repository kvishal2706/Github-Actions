name: first workflow
on: workflow_dispatch

jobs:
  first-job:
    runs-on: windows-latest
    steps:
      - name: Print Greeting
        run: echo "Hello world"
      - name: Print GoodBye!
        run: echo "GoodBye!!"
